[project]
name = "hirato-secretary"
version = "0.1.0"
requires-python = ">=3.11"
description = "Hirato Secretary — a channel-based LLM memory assistant"
dependencies = [
    "aiosqlite>=0.22.1",
]

[tool.poe.tasks]
dev = "uvicorn main:app --port 7950 --reload"
